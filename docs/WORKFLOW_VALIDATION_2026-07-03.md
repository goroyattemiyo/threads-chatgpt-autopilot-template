# Workflow validation

Validation trigger for the minute scheduler reliability changes on 2026-07-03 JST.

Checks:

- Python compile
- scheduler and thread resume tests
- checkpoint opt-in safety
- overdue audit exit status
- workflow YAML parsing
- repository configuration and schedule validation
